# Crow Buddy: Implementation Status & PAWS Review

**Date:** 2026-07-18
**Scope:** Full codebase audit — 25 Java files, ~2,200 LOC
**Last Updated:** 2026-07-18 (round 2 fixes applied)

---

## Implementation Status

| Component | Status | Notes |
|---|---|---|
| **Phase 1: Dependencies & Build** | ✅ Complete | Gradle, Fabric, GeckoLib, Loom all configured |
| **Phase 2: Registration** | ✅ Complete | Entities, blocks, items, sounds, networking all registered |
| **Phase 3: Core Mechanics** | ✅ Complete | Scavenging, swarm/distress, perching, taming, breeding trigger |
| **Phase 4: World & Environment** | ⚠️ Partial | Nest block/entity, spawning, features done; client-side incomplete |
| **Phase 5: Polishing** | ❌ Not Started | |

### What is implemented

- `CrowEntity` — full entity with AI goals, data sync, GeckoLib animation
- `ScavengeGoal` — priority-based item pickup with satiation cooldown
- `SwarmDistressGoal` — retaliation and swarm combat modes
- `SwarmManager` — singleton coordination with escalation tracking
- `CrowNestBlock` + `CrowNestBlockEntity` + `CrowNestStateMachine` — full lifecycle
- `CrowNestSeekGoal` — BFS-based nest finding (32-block radius)
- `CrowSpawning` + `CrowNestFeature` — biome spawning and procedural placement
- Networking — `DistressPayload`, `ScavengePayload` with codecs
- Registration — entities, blocks, items, sounds, features
- `BlackFeather` item — registered with `minecraft:feathers` tag
- `BLACK_OIL_SUNFLOWER_SEEDS` crafting recipe — shapeless: wheat_seeds + coal
- Crow entity loot table — black feather + seeds drops
- `CrowNestStateMachineTest` — full lifecycle unit test
- `CrowNestBlockEntityTest` — NBT serialization contract tests

### What is NOT yet implemented

- ~~Client-side network handlers are **empty stubs**~~ → ✅ **FIXED** (2026-07-18) — visual/audio feedback implemented
- ~~`BLACK_OIL_SUNFLOWER_SEEDS` crafting recipe (absent)~~ → ✅ **FIXED** (2026-07-18) — shapeless recipe: wheat_seeds + coal → 2 seeds
- ~~Crow entity loot/drop table (absent)~~ → ✅ **FIXED** (2026-07-18) — 0-2 black feathers always; seeds on player kill
- ~~`CrowNestBlockEntityTest` is a stub~~ → ✅ **FIXED** (2026-07-18) — NBT serialization contract tests implemented
- Block entity renderer for nest (commented out)
- Baby crow model registration (commented out)
- Custom particles (commented out)
- Sound OGG files (referenced but absent)
- Texture PNGs (`crow.png`, `crow_nest.png`, `icon.png` — absent)
- Mixins (both configs are empty)
- `getBreedOffspring()` returns `null` (by design — nest-based spawning)
- `onNonCrowDamaged` in SwarmManager is empty (by design)

---

## Fixes Applied (2026-07-18)

| # | Issue | Severity | Fix Applied | Files Changed |
|---|---|---|---|---|
| 1 | Cross-dimensional cooldown leakage | **HIGH** | Added `dimKey(level, crowId)` using `level.dimension()` as map key | `SwarmManager.java`, `SwarmDistressGoal.java` |
| 2 | Wall-clock escalation tracking | **MEDIUM** | Replaced `System.currentTimeMillis()` with `level.getGameTime()`; `ESCALATION_WINDOW_TICKS = 600` | `SwarmManager.java` |
| 3 | ArrayList race condition | **MEDIUM** | Replaced `ArrayList` with `CopyOnWriteArrayList` in `escalationHistory` | `SwarmManager.java` |
| 4 | BFS runs every AI tick | **MEDIUM** | Added `SEARCH_INTERVAL = 20` throttle; cached `targetPos` between searches | `CrowNestSeekGoal.java` |
| 5 | Duplicate `queue.add(start)` | **LOW** | Removed redundant second call | `CrowNestSeekGoal.java` |
| 6 | Empty client network handlers | **MEDIUM** | Distress: plays `CROW_DISTRESS` sound + `ANGRY_VILLAGER` particles. Scavenge: plays `ITEM_PICKUP` sound + `POOF` particles | `ModClientNetworking.java` |
| 7 | Reflection-based renderer registration | **MEDIUM** | Replaced `Class.forName()` with direct `EntityRendererRegistry.register()` | `ModClientEntities.java` |
| 8 | Goal priority conflict (priority 2) | **LOW** | `TemptGoal` → priority 3 (adults), `FollowParentGoal` → priority 3 (babies) | `CrowEntity.java` |
| 9 | Non-testable `CrowNestBlockEntity` | **LOW** | Constructor now accepts `BlockEntityType<?>` parameter; block passes type explicitly | `CrowNestBlockEntity.java`, `CrowNestBlock.java`, `ModBlocks.java` |
| 10 | Redundant sqrt in `followOwnerPerch()` | **LOW** | Compute `sqrt(distSq)` once as `dist`; reuse for speed + normalization | `CrowEntity.java` |
| 11 | `ScavengeGoal.start()` TOCTOU race | **LOW** | Added `isAlive()` guard before manipulating `ItemEntity` | `ScavengeGoal.java` |
| 12 | `SwarmDistressGoal` stale target window | **LOW** | `setTarget()` resets `lastHitTime` to current game time when target is Player | `SwarmDistressGoal.java` |

---

## PAWS Review by Component

### 1. `CrowEntity` (344 lines)

| Pillar | Rating | Findings |
|---|---|---|
| **P**erformance | ✅ Fixed | ~~`followOwnerPerch()` runs every tick while perched, computing `sqrt(distSq)` then `Math.min(sqrt(distSq) * 0.5, 0.3)` — redundant sqrt.~~ → **FIXED** (2026-07-18) — sqrt computed once, reused. |
| **A**uditability | ✅ Good | All state changes go through `entityData.set()`. Taming, perching, breeding are traceable. No debug logging in runtime path. |
| **W**orkability | ✅ Fixed | ~~`getBreedOffspring()` returns `null` — works because nesting handles spawning, but breaks if vanilla code calls it.~~ `registerGoals()` priority conflict ~~priority 3 is skipped (goals at 0,1,2,4,5,6). Baby crows get `FollowParentGoal` at priority 2, which conflicts with adult `TemptGoal` also at priority 2.~~ → **FIXED** (2026-07-18) — priorities resolved |
| **S**calability | ✅ Good | Clean separation of concerns. Goals are external classes. Animation controller is isolated. |

**Remaining:** `getBreedOffspring()` returns `null` (by design — nest-based spawning).

---

### 2. `SwarmManager` (287 lines)

| Pillar | Rating | Findings |
|---|---|---|
| **P**erformance | ✅ Fixed | ~~`escalationHistory` uses `System.currentTimeMillis()` — wall-clock time, not game ticks.~~ → **FIXED** (2026-07-18) — uses `level.getGameTime()`. `findNearbyCrows()` AABB inflate + re-filter remains (acceptable for small crow counts). |
| **A**uditability | ⚠️ Low | ~~Singleton `INSTANCE` with no dimension awareness — crows in different dimensions share cooldown state.~~ → **FIXED** (2026-07-18) — dimension-aware keys. No logging of swarm activation remains. |
| **W**orkability | ✅ Fixed | ~~Singleton is **not dimension-aware**. `escalationHistory` uses `ArrayList` (non-thread-safe) inside `ConcurrentHashMap.computeIfAbsent` — race condition.~~ → **FIXED** (2026-07-18) — `CopyOnWriteArrayList` + dimension keys. `onNonCrowDamaged()` remains empty (by design). |
| **S**calability | ⚠️ Low | Singleton pattern violates modularity. No way to unit-test without global state. Tight coupling to `CrowEntity` internals. |

**Remaining:** Singleton pattern (architectural), no swarm logging, `onNonCrowDamaged()` empty body.

---

### 3. `ScavengeGoal` (209 lines)

| Pillar | Rating | Findings |
|---|---|---|
| **P**erformance | ✅ Good | Uses `distanceToSqr` throughout. Search radius is 1.0 block (very small AABB). |
| **A**uditability | ✅ Good | Clear state transitions. Broadcasts `ScavengePayload` on pickup. |
| **W**orkability | ✅ Fixed | ~~`start()` directly removes `ItemEntity` without checking `itemEntity.isAlive()` — could fail if entity was already removed.~~ → **FIXED** (2026-07-18) — `isAlive()` guard added before manipulation. |
| **S**calability | ✅ Good | Self-contained goal class. No singleton dependencies. |

**Remaining:** None.

---

### 4. `SwarmDistressGoal` (207 lines)

| Pillar | Rating | Findings |
|---|---|---|
| **P**erformance | ✅ Good | Efficient tick-based distress sounds. Uses `distanceToSqr` for range check. |
| **A**uditability | ✅ Good | State transitions tracked via `CrowState.COMBAT`/`IDLE`. |
| **W**orkability | ✅ Fixed | ~~`performAttack()` redundant `Entity` cast. `checkTargetWindow()` may be stale if target switches.~~ → **FIXED** (2026-07-18) — `setTarget()` resets `lastHitTime` on target change. Redundant cast remains (cosmetic). |
| **S**calability | ✅ Good | Mode enum cleanly separates retaliation vs swarm. |

**Remaining:** Redundant `Entity` cast in `performAttack()` (cosmetic only).

---

### 5. `CrowNestStateMachine` (121 lines)

| Pillar | Rating | Findings |
|---|---|---|
| **P**erformance | ✅ Excellent | Simple integer state machine. O(1) transitions. |
| **A**uditability | ✅ Good | `sideEffectTriggered` flag provides clean one-shot notification. |
| **W**orkability | ✅ Good | Full lifecycle test covers all transitions. |
| **S**calability | ✅ Good | Pure state machine, no Minecraft dependencies. |

**Remaining:** None.

---

### 6. `CrowNestSeekGoal` (144 lines)

| Pillar | Rating | Findings |
|---|---|---|
| **P**erformance | ✅ Fixed | ~~BFS explores 274K blocks every AI tick.~~ → **FIXED** (2026-07-18) — throttled to once per 20 ticks with cached result. |
| **A**uditability | ✅ Good | Timeout mechanism prevents infinite searching. |
| **W**orkability | ✅ Fixed | ~~`queue.add(start)` called twice.~~ → **FIXED** (2026-07-18) — duplicate removed. `enterNest()` guarded by `STAGE_IDLE` check. |
| **S**calability | ✅ Good | Self-contained, no shared state. |

**Remaining:** None.

---

### 7. `CrowNestBlockEntity` (110 lines)

| Pillar | Rating | Findings |
|---|---|---|
| **P**erformance | ✅ Good | Efficient tick method with early return for IDLE stage. |
| **A**uditability | ✅ Good | `saveAdditional`/`loadAdditional` properly persist all state fields. |
| **W**orkability | ✅ Fixed | ~~**Tight coupling** to `ModBlocks.getCrowNestBE()` in constructor.~~ → **FIXED** (2026-07-18) — constructor accepts `BlockEntityType<?>` parameter. |
| **S**calability | ✅ Fixed | ~~Testability blocked by `ModBlocks` dependency.~~ → **FIXED** (2026-07-18) — injectable type parameter. `CrowNestBlockEntityTest` verifies NBT contract. |

**Remaining:** None.

---

### 8. Networking (`ModNetworking`, payloads)

| Pillar | Rating | Findings |
|---|---|---|
| **P**erformance | ✅ Good | Efficient serialization. |
| **A**uditability | ✅ Good | Proper `canSend` checks before sending. |
| **W**orkability | ✅ Fixed | ~~Client handlers are **empty stubs**.~~ → **FIXED** (2026-07-18) — visual/audio feedback implemented. |
| **S**calability | ✅ Good | Clean separation: server sends, client receives. |

**Remaining:** None.

---

### 9. `ModClientEntities` (15 lines)

| Pillar | Rating | Findings |
|---|---|---|
| **P**erformance | ✅ Fixed | ~~Uses `Class.forName()` + reflection.~~ → **FIXED** (2026-07-18) — direct Fabric API. |
| **A**uditability | ✅ Fixed | ~~Reflection failure only logged.~~ → **FIXED** (2026-07-18) — compile-time safety. |
| **W**orkability | ✅ Fixed | ~~**Fragile:** class name may change across MC versions.~~ → **FIXED** (2026-07-18) — direct import. |
| **S**calability | ✅ Fixed | ~~Reflection-based registration is a maintenance burden.~~ → **FIXED** (2026-07-18). |

**Remaining:** None.

---

## Updated Summary

| Component | P | A | W | S | Overall |
|---|---|---|---|---|---|
| `CrowEntity` | ✅ | ✅ | ✅ | ✅ | **Excellent** |
| `SwarmManager` | ✅ | ⚠️ | ✅ | ⚠️ | **Good** |
| `ScavengeGoal` | ✅ | ✅ | ✅ | ✅ | **Excellent** |
| `SwarmDistressGoal` | ✅ | ✅ | ✅ | ✅ | **Excellent** |
| `CrowNestStateMachine` | ✅ | ✅ | ✅ | ✅ | **Excellent** |
| `CrowNestSeekGoal` | ✅ | ✅ | ✅ | ✅ | **Excellent** |
| `CrowNestBlockEntity` | ✅ | ✅ | ✅ | ✅ | **Excellent** |
| Networking | ✅ | ✅ | ✅ | ✅ | **Excellent** |
| `ModClientEntities` | ✅ | ✅ | ✅ | ✅ | **Excellent** |

---

## Remaining Risk Assessment

| Risk | Severity | Component | Status |
|---|---|---|---|
| Cross-dimensional cooldown leakage in `SwarmManager` | ~~**HIGH**~~ | `SwarmManager` singleton | ✅ **FIXED** |
| BFS performance (274K blocks per tick) | ~~**MEDIUM**~~ | `CrowNestSeekGoal` | ✅ **FIXED** |
| Reflection-based renderer registration | ~~**MEDIUM**~~ | `ModClientEntities` | ✅ **FIXED** |
| Goal priority conflict (priority 2) | ~~**LOW**~~ | `CrowEntity.registerGoals()` | ✅ **FIXED** |
| Empty client network handlers | ~~**MEDIUM**~~ | `ModClientNetworking` | ✅ **FIXED** |
| Non-testable `CrowNestBlockEntity` | ~~**LOW**~~ | Tight coupling to `ModBlocks` | ✅ **FIXED** |
| Redundant sqrt in `followOwnerPerch()` | ~~**LOW**~~ | `CrowEntity` | ✅ **FIXED** |
| `ScavengeGoal.start()` missing `isAlive()` check | ~~**LOW**~~ | TOCTOU race | ✅ **FIXED** |
| `SwarmDistressGoal` stale target window | ~~**LOW**~~ | Target switch edge case | ✅ **FIXED** |
| **Missing assets (textures, sounds)** | **HIGH** | Build will fail at runtime | ⏳ **REMAINS** |
| **Singleton pattern in SwarmManager** | **LOW** | Architectural concern | ⏳ **REMAINS** |

---

## Remaining Work

### From "What is NOT yet implemented"

These items were not code-quality issues but missing features/assets:

- Block entity renderer for nest (commented out)
- Baby crow model registration (commented out)
- Custom particles (commented out)
- Sound OGG files (referenced but absent)
- Texture PNGs (`crow.png`, `crow_nest.png`, `icon.png` — absent)
- Mixins (both configs are empty)
- `getBreedOffspring()` returns `null` (by design — nest-based spawning)
- `onNonCrowDamaged` in SwarmManager is empty (by design)

### From "Recommended Improvements" — Remaining

| # | Recommendation | Status |
|---|---|---|
| 1 | Fix `SwarmManager` — dimension keys, game ticks | ✅ **DONE** |
| 2 | Throttle BFS in `CrowNestSeekGoal` | ✅ **DONE** |
| 3 | Complete client handlers | ✅ **DONE** |
| 4 | Replace reflection in `ModClientEntities` | ✅ **DONE** |
| 5 | **Add assets** — Sound OGGs, texture PNGs | ⏳ **REMAINS** |
| 6 | Refactor `CrowNestBlockEntity` constructor | ✅ **DONE** |
| 7 | Fix goal priority conflict in `CrowEntity` | ✅ **DONE** |

**Only item #5 remains** from the prioritized recommendations list (asset creation).

---

**Confidence Level:** High — All 12 code-quality fixes verified via `./gradlew compileJava compileClientJava` and `./gradlew test` (9 tests pass). New `CrowNestBlockEntityTest` avoids Minecraft mocks (unavailable in Loom test environment) and verifies NBT serialization contract directly.
